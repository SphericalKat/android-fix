<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/sarHax</item>
        <item name="colorPrimaryDark">@color/sarHax</item>
        <item name="colorAccent">@color/sarHax</item>
        <item name="font">@font/hacked</item>
    </style>

</resources>